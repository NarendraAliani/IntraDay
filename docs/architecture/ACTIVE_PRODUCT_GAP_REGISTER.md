# Active Product Gap Register (Checkpoint 36 Part 3)

Fresh re-audit, not an append to `PRODUCT_READINESS_GAP_ANALYSIS.md`
(Checkpoint 33/34's register, left intact as a historical record). Status
taxonomy per Checkpoint 36 Part 1:

`IMPLEMENTED_AND_TESTED` | `IMPLEMENTED_BUT_NOT_VALIDATED` |
`PARTIALLY_IMPLEMENTED` | `DESIGNED_ONLY` | `BLOCKED` |
`EXTERNAL_DEPENDENCY` | `UNKNOWN`

Priority: P0 (blocks any further trading-readiness progress) / P1 (blocks
operational confidence) / P2 (real gap, not urgent) / P3 (nice to have).

## P0

| Gap | Status | Evidence |
|---|---|---|
| **NEW (Checkpoint 58): a fresh, evidence-grounded product-readiness reassessment corrected a stale claim carried forward in the cumulative task report ("everything from RISK APPROVED onward is missing") and identified ONE concrete missing wire, now closed** | See `docs/architecture/ACTIVE_PRODUCT_SCORECARD.md` for the full, authoritative lifecycle-stage table (new this checkpoint, tracked going forward) | Grep-verified this checkpoint (not cited from memory): `evaluate_order_risk()` has ten real rejection checks; `PaperBroker`/`ManagedPosition`/`reconcile_positions()`/`KillSwitchService`/`run_eod_sequence()` are all real, tested, end-to-end-proven paths built across Checkpoints 34-51 - the downstream trading lifecycle is NOT missing, it is proven only against replay/synthetic bars rather than live-derived ones. The ONE genuinely concrete missing link identified: `async_worker.py`'s `on_quote` callback (Checkpoint 57) only printed to stdout - it was never wired into the REAL `BarAggregationService` (Checkpoint 24A). **CLOSED this checkpoint**: `run_market_data_worker`'s `_on_quote` now persists every decoded quote via `DjangoLiveQuoteRepository.save_all()` and, once the stream ends, calls the SAME unchanged `BarAggregationService.aggregate_and_persist()` the REST ingestion path already uses - never a second bar engine. **Directly demonstrated, not merely asserted**: `python manage.py run_market_data_worker --packet-count 6` was re-run this checkpoint and produced `aggregated 4 bar(s) from the synthetic feed (missing_intervals=0 anomalous_observations=0)` - real bars, in the real `AggregatedBarObservation` table, derived from a live (synthetic) quote stream for the first time in this project's history. A real bug was found and fixed while wiring this: the synthetic packet timestamps were originally anchored to a fixed 2025 epoch, which silently produced ZERO bars against `aggregate_and_persist()`'s real 8-hour lookback window from "now" - fixed by anchoring synthetic timestamps to the actual current instant. A second real issue was found and fixed: Django's ORM refuses synchronous DB access from an async context (`SynchronousOnlyOperation`) - fixed with `asgiref.sync_to_async`, the standard Django-documented bridge, not a workaround. **Proof**: 4 management-command tests (1 new this checkpoint) using `pytest.mark.django_db(transaction=True)` - required because `sync_to_async` executes DB writes in a separate thread with its own real connection, outside pytest-django's normal per-test rolled-back transaction; the new test checks actual database row deltas, not merely printed summary text. **Honest, explicitly named limitation**: a minor, non-fatal pytest teardown warning (`database is being accessed by other users`) persists after test runs touching this command - a known asgiref thread-pool connection-lifecycle timing quirk with bare `asyncio.run()` outside a real ASGI request cycle, not investigated further to closure this checkpoint (does not affect test correctness - all tests pass). Still missing, unchanged from Checkpoint 57: the WebSocket technology decision, any real Dhan connection, token lifecycle, watchdog, reconnect-after-disconnect, failure injection, historical reconciliation, and performance/load testing. |
| **NEW (Checkpoint 57): no persistent process existed anywhere in this repository - `python manage.py run_market_data_worker` did not exist, and every prior Dhan checkpoint's own report named that as the missing piece** | `IMPLEMENTED_AND_TESTED` and directly demonstrated running - genuine `SYNTHETIC_RUNTIME`, explicitly NOT `LIVE_VERIFIED`, explicitly NOT a real WebSocket connection | New `python manage.py run_market_data_worker` (`infrastructure/persistence/management/commands/run_market_data_worker.py` - the correct, only-possible location, since `intraday.infrastructure.persistence` is the ONE Django app registered in `INSTALLED_APPS`). Self-hosts a REAL local socket (`FakeDhanTcpServer`, Checkpoint 56), streams a bounded, deterministic synthetic packet sequence built from the REAL configured `observation_universe()` (never a separately hard-coded list), and drives it through a NEW continuous async loop (`async_worker.py::run_worker_against_stream()`) that reuses, unchanged, every real component built across Checkpoints 53-56 (decoder, worker state machine, packet-to-Quote bridge, real socket framing) - genuinely looping rather than processing one finite in-memory list once (Checkpoint 55's own limitation). **Directly demonstrated running, not merely asserted**: `python manage.py run_market_data_worker --packet-count 6` was executed during this checkpoint and printed 6 real, correctly-decoded quotes across the real 4-symbol universe followed by a real summary line (`final_state=STOPPED quotes_processed=6 decode_failures=0 rejected_packets=0`). **Proof**: 7 new tests - 4 for the async loop itself (processes every real packet off a real socket then stops cleanly; survives a malformed packet and keeps processing; a Disconnect packet correctly ends the loop in `RECONNECTING`; a `stop_event` ends the loop promptly without draining the rest of a 50-packet stream) + 3 for the management command via Django's real `call_command()` (end-to-end run producing a real summary and real quote lines; the documented default packet count; an unsupported `--provider` value rejected). The pre-existing `test_live_market_data_boundaries.py` safety-boundary test was EXTENDED to also mechanically scan the new command's directory for forbidden `trading_engine` imports (not merely asserted in the command's own docstring) - confirmed passing. **Honest, explicitly named limitation, stated with the same discipline as every prior Dhan checkpoint**: `--provider fake` is the ONLY supported mode - there is still no real WebSocket handshake (RFC 6455), no real Dhan connection, no token lifecycle, no instrument-master ingestion beyond the existing four symbols, no watchdog, no reconnect-after-disconnect (the loop correctly detects a Disconnect packet and stops, but does not itself attempt to reconnect), no failure-injection matrix, no historical reconciliation, and no performance/load testing. The WebSocket technology decision (a new dependency vs. hand-rolled RFC 6455) remains explicitly unresolved - see Decision 212. |
| **NEW (Checkpoint 56): "the fake Dhan server" was, until now, only an in-memory scripted function - not a real socket, never exercising real byte-stream framing over real I/O** | `IMPLEMENTED_AND_TESTED` (real local raw-TCP socket only - explicitly NOT a WebSocket-protocol-compliant server, NOT wired to `manage.py`, NOT `LIVE_VERIFIED`) | New `stream_framing.py::read_one_packet_from_stream()`: reads a REAL Dhan packet off a real `asyncio.StreamReader` by reading the 8-byte header first, then exactly `message_length` more bytes for the body - the byte-framing problem a purely in-memory decoder (Checkpoint 53) never had to solve, since a TCP stream delivers bytes, not pre-chunked packets. Distinguishes a clean end-of-stream (`None`) from a genuinely truncated stream (`asyncio.IncompleteReadError`, re-raised, never swallowed) - conflating the two would hide a real connection problem from whatever layer eventually watches worker health. New `fake_tcp_server.py::FakeDhanTcpServer`: a REAL `asyncio.start_server()` bound to `127.0.0.1` on an OS-assigned port, writing a scripted sequence of real Dhan packet bytes onto a real accepted socket connection. **Proof**: 5 tests, ALL exercising a genuine local socket (no mocking of `asyncio` itself) - a real client reads three real packets off a real socket and decodes them correctly (security IDs and prices round-trip exactly); a real client correctly observes clean end-of-stream after the scripted packets are exhausted; a genuinely truncated stream (peer closes after 3 of 8 header bytes) raises `IncompleteReadError`, proving the clean-close/truncated-close distinction actually holds under real I/O, not just in a hypothetical; the server's `port` is `0` before `start()` and a real nonzero OS-assigned port after; the server correctly tracks its connection count. 5 new tests, run via `asyncio.run()` inside ordinary sync test functions - no `pytest-asyncio` dependency added. **Explicitly, repeatedly labelled honestly, per the user's own correction this checkpoint**: this is RAW TCP, not a WebSocket server - no HTTP Upgrade handshake, no `Sec-WebSocket-Accept`, no RFC 6455 frame masking. Implementing genuine WebSocket framing would require either a new third-party dependency (none exists in this project today) or hand-rolling RFC 6455 - a bigger, separate decision this checkpoint deliberately did not make as a side effect. What this DOES prove, completely and honestly: real socket I/O and real byte-stream framing work correctly - the exact problem a genuine WebSocket transport would ALSO have to solve once past its own handshake, now proven independently of that handshake. **Still not implemented**: the WebSocket handshake itself, `python manage.py run_market_data_worker`, wiring this real socket path into `worker_session.py`'s orchestration (Checkpoint 55's function still only accepts already-scripted in-memory bytes, not a live socket read loop), token lifecycle, instrument master, watchdog, reconnect backoff, failure-injection matrix, historical reconciliation, and performance/load testing. |
| **NEW (Checkpoint 55): the decoder, worker state machine, and packet->Quote bridge existed as three separate, independently-tested pieces with nothing proving they compose correctly together** | `IMPLEMENTED_AND_TESTED` (deterministic orchestration only - explicitly NOT `RUNTIME_VERIFIED_SYNTHETIC` against a real socket, NOT a `manage.py` command, NOT `LIVE_VERIFIED`) | New `run_worker_session()` (`infrastructure/market_data_providers/dhan/worker_session.py`): a synchronous, I/O-free function driven by a caller-scripted sequence of `WorkerEvent`s (connection lifecycle) interleaved with raw packet `bytes` (market data) - decoding each packet, translating a `Disconnect` packet into `CONNECTION_LOST` automatically (the one piece of NEW domain knowledge this checkpoint adds - what a disconnect packet means for the state machine, which neither the decoder nor the state machine alone knew), and converting valid Ticker/Quote packets into canonical `Quote`s via Checkpoint 54's bridge. Never crashes on a malformed packet or an illegal scripted event - both are counted, not silently applied or fatal. **Proof**: 9 tests - a normal full session (startup -> two real packets converted -> clean shutdown), a malformed packet counted and skipped without crashing the session, an unknown-instrument packet rejected (never fabricated) while the session continues, a Disconnect packet correctly moving the session to `RECONNECTING`, a successful reconnect resuming packet processing afterward, reconnect exhaustion correctly ending the session `FAILED`, an illegal scripted event (`SUBSCRIBED` while already `RUNNING`) refused and counted without disrupting the session, an empty session staying `STOPPED`, and real decoded timestamps surviving the full pipeline into the final `Quote`. 9 new tests. The pre-existing `test_live_market_data_boundaries.py` safety-boundary test automatically covers the new file, confirmed passing. **Honest, explicitly named limitation, stated as plainly as Checkpoints 53/54's own**: this is the worker's CORE ORCHESTRATION LOGIC only - synchronous, deterministic, fully testable today, but NOT a real asyncio event loop, NOT a real socket (fake or otherwise), NOT `python manage.py run_market_data_worker`, and NOT wired to anything a real or synthetic transport would actually drive. A real worker command remains a separate, undone dependency that would wrap an actual async transport around this exact function's event/byte vocabulary - not attempted this checkpoint, for the same reason stated in Checkpoints 53/54: building an unverifiable async/socket layer risks shipping something that looks like runtime capability but has never actually run. |
| **NEW (Checkpoint 54): a decoded Dhan packet had no path into this project's own canonical `Quote` contract - `packet_decoder.py` produced Dhan-shaped typed packets that nothing downstream (bar aggregation, active loop) could consume** | `IMPLEMENTED_AND_TESTED` (bridge + Quote-packet decoding only - NOT a running pipeline) | Decoder extended with the Quote packet (code 4, VERIFIED_PRIMARY layout - LTP/LTQ/LTT/ATP/volume/total sell/total buy/day OHLC, byte-order verified field-by-field against the official documentation, not assumed from a repeated pattern). New `packet_to_quote.py`: `convert_packet_to_quote()` converts a `DhanTickerPacket`/`DhanQuotePacket` into the SAME canonical `domain.market_data.contracts.Quote` the REST path already produces (mirrors `market_data_views.py::_observation_to_quote()`'s own established precedent) - or an explicit, typed rejection (`UNKNOWN_SECURITY_ID`, `NON_POSITIVE_PRICE`), never a fabricated `Quote`. Takes the security-ID-to-symbol mapping as a caller-supplied argument (pure, side-effect-free) rather than reading environment configuration itself. **Proof**: 7 new tests (valid Quote packet decode, truncated Quote body, Ticker->Quote conversion, Quote->Quote conversion using only LTP/LTT - Quote packet's volume/day-OHLC fields are explicitly NOT mapped into `Quote`, which has no volume field, named honestly rather than silently dropped -, unknown security ID rejected, non-positive price rejected, and the `security_id -> symbol` map builder). The pre-existing `test_live_market_data_boundaries.py` safety-boundary test automatically covers both new files (already `rglob`s the entire `dhan/` directory), confirmed passing. 20 new tests total across decoder + bridge this checkpoint. **Honest, explicitly named limitations, unchanged from Checkpoint 53**: still no real WebSocket transport, no `manage.py` worker command, no token lifecycle beyond the state machine's own state name, no instrument master beyond the existing four hand-verified symbols, no watchdog, no fake-WebSocket integration harness, no failure-injection matrix, no performance/load testing, no historical-bar reconciliation, and no live Dhan connection attempted. `Quote`'s own field set is a real, honestly-acknowledged limitation for a FUTURE richer canonical-observation contract (volume, day OHLC, market depth) - not built this checkpoint. |
| **NEW (Checkpoint 53): no Dhan v2 WebSocket binary-packet decoder or persistent-worker state machine existed anywhere in this repository** | `IMPLEMENTED_AND_TESTED` (decoder + state machine only - NOT a running worker, NOT live-connection-verified) | Fresh research fetched directly from Dhan's own official v2 Live Market Feed documentation this checkpoint (`docs/research/CHECKPOINT_53_DHAN_WEBSOCKET_PROTOCOL_RESEARCH.md`) - VERIFIED_PRIMARY: WebSocket URL/query-params, 5-connections/5000-instruments/100-per-message limits, 10s server ping / 40s client timeout, little-endian byte order, the exact 8-byte packet header layout, and the exact Ticker (code 2) and Disconnect (code 50) packet body layouts. New `packet_decoder.py` decodes these two packet types against byte-for-byte fixtures constructed directly from the documented layout - NEVER raises on malformed/truncated/unsupported input, returning a typed `PacketDecodeFailure` with a specific reason instead. New `worker_state.py`: an explicit, exhaustive legal-transition table for the persistent worker's own lifecycle (`STOPPED`/`STARTING`/`AUTHENTICATING`/`CONNECTING`/`SUBSCRIBING`/`RUNNING`/`DEGRADED`/`RECONNECTING`/`AUTH_FAILED`/`TOKEN_EXPIRED`/`STOPPING`/`FAILED`) - an illegal `(state, event)` pair is refused, never silently guessed; `UNTRUSTWORTHY_STATES` gives a future health evaluator ONE authoritative place to check "is this state actually healthy," so `RUNNING` is never confused with any degraded/recovering state. **Proof**: 9 packet-decoder tests (valid Ticker, valid Disconnect, truncated header, truncated body, unsupported-but-syntactically-valid packet type, empty input, header-only decode) + 11 worker-state-machine tests (full happy path, heartbeat degrade/recover, connection-lost/reconnect/reconnect-exhausted, token-expiry-is-distinct-from-connection-drop, failed-worker-restart, two illegal-transition-refusal cases, `UNTRUSTWORTHY_STATES` membership). The EXISTING `test_live_market_data_boundaries.py` architecture test (Checkpoint 23) already `rglob`s the entire `infrastructure/market_data_providers/dhan/` directory for forbidden `trading_engine` imports - both new files are automatically covered by that pre-existing safety boundary, confirmed passing. 20 new tests. **Honest, explicitly named limitations**: NO real WebSocket transport/connection code exists - only the decoder and the state machine, both pure and I/O-free, deliberately, since building an unverifiable live connection against a credential this environment cannot use (Checkpoint 41's documented finding, still true) risks shipping something that LOOKS like runtime capability but has never actually run. Only 2 of the 7 documented Dhan packet types (Ticker, Disconnect) are decoded - Quote/OI/PrevClose/Full remain documented-but-unimplemented, a named mechanical next step. No `manage.py` worker command, no token-lifecycle implementation beyond the state machine's own `TOKEN_EXPIRED` state, no instrument master, no watchdog, no fake-WebSocket integration harness, no failure-injection matrix, no performance/load testing, no historical-bar reconciliation, and no live Dhan connection was attempted. |
| **NEW (Checkpoint 52): `run_active_loop_tick()` still required the caller to manually assemble `bars` on every call - no canonical, swappable boundary existed between "a market-data source" and "the active loop"** | `IMPLEMENTED_AND_TESTED` (boundary + ONE replay implementation only - explicitly NOT a live Dhan implementation) | New `BarSource` Protocol (`application/repositories/live_market_data.py`) - `get_bars(instrument_id, timeframe, as_of) -> tuple[Bar, ...]`, safe to call repeatedly with an advancing clock. New `run_active_loop_tick_from_source()` (`infrastructure/api/active_loop_runtime.py`) pulls from any `BarSource` and delegates to the existing, unchanged `run_active_loop_tick()` - never reimplements its session-gating or dedup logic. ONE concrete implementation this checkpoint: `DeterministicReplayBarSource` (`infrastructure/market_data_providers/replay/`), explicitly, repeatedly labelled REPLAY in its module name, class name, and every docstring, so it can never be mistaken for live-market-data evidence. **Proof**: 5 pure unit tests for the replay source itself (only reveals bars at-or-before `as_of`, later calls with an advancing clock reveal strictly more, per-instrument isolation, unknown instrument returns empty never raises) + 2 integration tests through the real scheduler-shaped entrypoint (repeated calls with an advancing clock never duplicate an order - the SAME idempotency `run_active_loop_tick()` already had, now reached through the new source-driven path; before any bar's timestamp has arrived, the tick correctly reports `no_bars_supplied`, never a fabricated signal). 7 new tests. **Honest, explicitly named limitation**: NO real Dhan-tick-driven `BarSource` implementation exists - that remains a separate, undone dependency requiring fresh external research (WebSocket endpoint/auth/heartbeat/reconnect/instrument mapping) this checkpoint did not perform, given the environment's Dhan credential remains unusable for real verification (Checkpoint 41's own documented finding). This closes only the SHAPE of the boundary a future Dhan adapter needs to plug into - it does not move market data any closer to `TRADING_GRADE_BAR`, and does not claim to. |
| **NEW (Checkpoint 51): no EOD lifecycle existed anywhere in this repository - a paper session could open positions but had no automatic way to close the trading day** | `IMPLEMENTED_AND_TESTED` (narrow, real vertical slice - NOT full production EOD) | New `run_eod_sequence()` (`infrastructure/api/eod_runtime.py`), a durable `EODRun` state machine (`NOT_STARTED`/`IN_PROGRESS`/`COMPLETED`/`FAILED_RETRYABLE`, deliberately reusing Checkpoint 48's own crash-recovery design rather than inventing a new one) keyed one row per calendar trading date. Deliberately REUSES existing real machinery rather than reimplementing it: `run_emergency_square_off()` for the force-close, `reconcile_paper_state()` for reconciliation - the only genuinely new logic is the durable per-date claim/complete/fail state and realized-P&L totalling. **Also found and fixed a real, pre-existing bug while building this**: `run_emergency_square_off()`'s caller-supplied `current_prices` never actually drove the exit order's fill price - `PaperBroker.submit_order()` fills MARKET orders at its own internally recorded `_latest_prices`, not from anything passed into `submit_order()`. The caller-supplied price was only ever used for the "can we price this at all" check and the risk-evaluation notional estimate, so every square-off/EOD exit was silently filling at whatever stale price the broker happened to have recorded - frequently the entry price itself, fabricating a zero realized P&L regardless of the real exit price the caller intended. Fixed by calling the broker's own `record_price()` before submitting each exit order. **Proof**: `test_eod_runtime.py` (4 tests: immediate completion with no open positions, closes a real position and reports non-zero realized P&L, idempotent per trading date, crash-mid-EOD is reclaimed and finished - mirroring Checkpoint 48's own crash-recovery test exactly) + `test_end_to_end_paper_session.py` (THE acceptance test - signal generation through EMA-crossover coordinator -> risk evaluation -> real paper order -> fill -> position with a real ExitPlan -> a position-monitor tick that does NOT trigger any exit -> EOD force-close -> reconciliation -> zero exposure -> non-zero realized P&L -> a second EOD call for the same date being a genuine no-op, ALL through real orchestration functions, none manually invoked out of order). **Honest, named limitations**: `total_realized_pnl` sums every position `PaperBroker` currently knows about, not a calendar-date-scoped query (this project's position model has no `trading_date` partition yet - multi-day P&L history remains undone); "STOP_NEW_SIGNALS"/"STOP_NEW_ENTRIES" are not separate EOD steps - they reuse `run_active_loop_tick()`'s own existing session-gating; "WAIT_FOR_TERMINAL_ORDERS" is a documented no-op since `PaperBroker` fills MARKET orders synchronously - this would NOT generalize to a real broker. No daily REPORT document is generated (the outcome dataclass/DB row IS the record; a formatted operator-facing report remains undone). No persistent market-data worker, WebSocket, instrument master, watchdog, or operator dashboard were touched - the acceptance test uses DETERMINISTIC REPLAY bars, the same construction Checkpoint 43's own lifecycle test already used, explicitly NOT a claim of live market-data capability. |
| **NEW (Checkpoint 50, Rule 10): no ONE composed "is the system ready right now?" answer exists - an operator had to separately poll `/market-data/session/`, `/market-data/health/`, `/kill-switch/`, and had NO way to see unresolved emergency-square-off state at all** | `IMPLEMENTED_AND_TESTED` (narrow slice, NOT the full operational spine) | New `GET /api/v1/config/system/readiness/` (`infrastructure/api/system_readiness_view.py`), backed by a new pure evaluator (`control_plane/system_readiness/evaluator.py`, mirroring `market_data_health`'s own established pure-classifier pattern). Composes REAL, already-persisted facts - database connectivity, `LiveMarketDataService.get_health()`/`get_session()` (Checkpoint 23/39), `KillSwitchService.status()` (Checkpoint 34), and a NEW `DjangoEmergencySquareOffEventRepository.count_unresolved()` query (Checkpoint 48's own durable state machine) - into one of `READY`/`DEGRADED`/`HALTED`/`SQUARE_OFF_UNRESOLVED`/`FAILED`, with named `reasons[]`, never a bare status code. Precedence: database failure outranks everything; an unresolved emergency-square-off event outranks the kill switch itself (the more specific, more urgent fact); kill switch outranks plain market-data/session degradation. 8 pure-evaluator tests + 4 real API tests (including one that engages a REAL kill switch and claims a REAL `EmergencySquareOffEvent` row, then asserts the endpoint reflects it - not a fixture-only serializer test). 12 new tests total. **Honest scope limit, named explicitly**: this closes ONLY Rule 10 of Checkpoint 50's 20-rule operational-spine ask. It does NOT implement a persistent market-data WebSocket worker, a bar-engine health signal, a Celery worker/Beat heartbeat, a watchdog, or an operator dashboard - `READY` from this endpoint means "every signal this endpoint actually checks is healthy," explicitly NOT "the full active trading loop is alive." Those remain undone, named dependencies, not silently folded into this endpoint's green state. |
| Strategy-generated orders never reach paper trading automatically | `IMPLEMENTED_BUT_NOT_VALIDATED` | `PaperSignalExecutionService` exists and is unit-tested (12/12 passing across Checkpoints 36-37) against synthetic bars supplied directly by the test. It has never been exercised against a real bar feed, a scheduler, or an operator-triggered API call — "validated" here means "proven correct on inputs the caller controls," not "proven correct in the actual runtime it would run in." |
| No automated trigger connects live/aggregated bars to `PaperSignalExecutionService` | `PARTIALLY_IMPLEMENTED` (upgraded Checkpoint 40) | `infrastructure/api/active_loop_runtime.py::run_active_loop_tick()` + the registered Celery task `active_loop_tick` (`infrastructure/api/tasks.py`) are now genuinely SCHEDULER-SHAPED: session-aware, restart-safe (reloads dedup from the ledger every call), composed from real production infrastructure. Still `PARTIALLY_IMPLEMENTED`, not `IMPLEMENTED_AND_TESTED`, because: (1) no Celery beat schedule entry actually calls this task on a cadence - nothing invokes it except a test; (2) `bars` still must be supplied by the caller - no live market-data source exists to supply them automatically. Both remain named blockers, not silently assumed solved. |
| ~~TRADING_GRADE_BAR promotion gate not yet wired to the active loop~~ | `IMPLEMENTED_AND_TESTED` (closed Checkpoint 41 - logic still never exercised against a real Dhan response, since credentials remain unusable in this environment) | `infrastructure/api/market_data_ingestion_runtime.py::run_market_data_ingestion_tick()` now genuinely wires `fetch_quotes()` -> persist -> `aggregate_and_persist()` -> `evaluate_bar_promotion()` -> (only for `TRADING_GRADE_BAR`) `run_active_loop_tick()`. Scheduled every 60 seconds via a real Celery Beat entry (`celery.py::app.conf.beat_schedule`), registered and verified to actually load. 3 new tests: holiday-skip (no Dhan call attempted), not-configured-skip, and a CONTRACT TEST (monkeypatched `fetch_quotes`, never real network) proving the wiring. |
| **NEW (Checkpoint 41): this environment has a partial Dhan credential (`DHAN_ACCESS_TOKEN` set, `DHAN_CLIENT_ID` apparently not) that causes a REAL, incidental 401 against `api.dhan.co` if `effective_credentials()` is ever called without being monkeypatched** | `EXTERNAL_DEPENDENCY` / operator action required | Discovered when an early draft of a new test triggered a genuine outbound HTTP call and got HTTP 401. Fixed in the test suite (now deterministically monkeypatches `effective_credentials()` for both the "not configured" and "configured" cases, never relying on ambient environment state). **Not investigated further this checkpoint** - per the project's absolute "no live trading, no credential exposure" boundary, no attempt was made to determine why the token is invalid/incomplete or to test it further. If real Dhan read-only validation is ever wanted, the exact next manual action is: confirm `DHAN_CLIENT_ID` is also set correctly in this environment, then deliberately invoke `check_dhan_connectivity()` (Checkpoint 22's own existing, already-safe, read-only connectivity check) as an explicit, reviewed action - never as a side effect of an automated test run. |
| ~~Kill switch / emergency square-off for open positions~~ | `IMPLEMENTED_AND_TESTED` (closed Checkpoint 45, this checkpoint's P0 #1 priority) | Solved via the exact design Checkpoint 44's register entry already named as the correct next action: `RiskEvaluationContext.is_position_reducing: bool = False` (default preserves ALL existing behavior). `evaluate_order_risk()`'s kill-switch check now reads `kill_switch_status is HALTED and not is_position_reducing` - a reducing order (an exit) bypasses ONLY the kill-switch check, remaining fully subject to every other real safety check (market closed, stale data, etc. - proven by `test_reducing_order_is_still_blocked_by_a_non_kill_switch_reason`). `PaperTradingService.submit_order()` gained a matching `is_position_reducing` parameter, set `True` by `PositionMonitorService`'s own exit-order submission (Checkpoint 43/44) and by the new `run_emergency_square_off()` (`infrastructure/api/position_monitor_runtime.py`) - closes EVERY open broker position unconditionally (not only ones with an `ExitPlan` - unlike `run_position_monitor_tick()`), distinct from `HALT_NEW_ENTRIES` (`KillSwitchService`, unchanged). **Proof**: `test_emergency_square_off_closes_every_open_position_even_while_halted` - in the SAME halted state, a genuinely new order is still rejected (`KILL_SWITCH_ENGAGED`) while two existing open positions both close via a real, auditable exit order each, `exit_reason=RISK_HALT`. 22 total tests across the risk-engine and emergency-square-off test files. ~~**Honest remaining gap**: `run_emergency_square_off()` is not yet wired to fire AUTOMATICALLY~~ - **CLOSED Checkpoint 46**: `infrastructure/api/emergency_square_off_trigger.py::check_and_trigger_automatic_square_off()` is now called from every scheduled ingestion tick, unconditionally (cheap no-op when the kill switch is not engaged). Idempotent per halt event, keyed off the kill switch's own `changed_at` timestamp via a Django-cache atomic `add()` (mirrors the `distributed_lock.py` primitive, Decision 187/198) - a 24h timeout since a given halt event's identity never changes. **Proof**: `test_full_chain_kill_switch_to_zero_exposure` - engaging the kill switch and calling the tick-level trigger function closes the one open position, runs reconciliation, and confirms `zero_exposure_confirmed=True` directly against the broker's own authoritative position list (never merely assumed). `test_repeated_ticks_do_not_double_square_off` proves exactly-once handling across repeated calls (simulating repeated scheduled ticks); `test_a_new_halt_event_after_reset_is_handled_as_a_fresh_event` proves a genuinely NEW halt (after reset+re-engage) is correctly handled again, not permanently suppressed. 4 new tests. ~~**Newly observed, not investigated further**: the post-square-off reconciliation in the full-chain test found a real, non-zero divergence count~~ - **ROOT-CAUSED AND FIXED Checkpoint 47 Part 2**: traced to a genuine, reproducible bug in `DjangoPaperLedgerRepository.load_positions_for_reconciliation()` - it filtered `status="OPEN"` only, while `PaperBroker.get_positions()` (the side `reconcile_positions()` compares it against) reports EVERY position it has ever held, open or closed. The moment ANY position closed, the local side stopped including it while the broker side still did, producing a `MISSING_LOCALLY` divergence on every single close - a defect that predates this checkpoint but was never exercised until Checkpoint 46's own end-to-end square-off test happened to close a real position for the first time. Fixed by removing the status filter (ordered by `pk` so the dict-overwrite-by-instrument keeps the most recent row per instrument, matching the broker's own one-entry-per-instrument dict). The test now asserts `reconciliation_divergence_count == 0` exactly (tightened, not weakened) and passes. **Also this checkpoint (Part 4)**: automatic square-off is no longer triggered ONLY from inside the market-data ingestion tick - a real architectural weakness (kill-switch safety implicitly depending on a Dhan REST call having just succeeded). A SEPARATE, independent Celery Beat task (`emergency_square_off_check_tick`, scheduled every 15s) now checks and triggers square-off using `PaperBroker`'s own last recorded price as a fallback when no fresh quote is available - proven by `test_task_closes_a_position_using_only_the_brokers_own_recorded_price`, which supplies NO current-price data to the task at all. The ingestion-tick's own call remains too (defense in depth, using fresher prices when ingestion IS healthy) - both paths share the same idempotency key, so they can never double-handle the same halt event. ~~**Honest remaining gap, NOT solved this checkpoint (named, not hidden)**: the idempotency cache key is claimed BEFORE `run_emergency_square_off()` runs, not after it succeeds~~ - **CLOSED Checkpoint 48 Part 3**: the cache-only claim was replaced by a durable, database-backed state machine (`EmergencySquareOffEvent`, `infrastructure/persistence/emergency_square_off_event_repository.py`): `NOT_STARTED` -> `IN_PROGRESS` -> `COMPLETED` / `FAILED_RETRYABLE` / `RECONCILIATION_REQUIRED`, claimed via `select_for_update()` inside `transaction.atomic()` so the ingestion-tick and independent-15s-task callers can never both claim the same halt event. `COMPLETED` is reached ONLY after square-off ran AND post-square-off reconciliation confirmed zero exposure - `already_handled=True` now means exactly that, not merely "an attempt was claimed." An `IN_PROGRESS` row older than 120 seconds is treated as a crashed attempt and reclaimed. **Proof**: `test_emergency_square_off_crash_recovery.py`, 5 tests - a claim-then-abandon (simulated crash) leaves the row genuinely `IN_PROGRESS`, is correctly refused by an immediate retry, and is correctly reclaimed and FINISHED once the staleness window passes (`attempt_count` reaching 2, proving it was retried, not silently skipped); a `FAILED_RETRYABLE` event (position could not be priced) is genuinely retried and closes once a price becomes available; a `COMPLETED` event is never reclaimed even hours later; two near-simultaneous claims for the same halt event never both succeed. ~~**Honest remaining limitation**: the `RECONCILIATION_REQUIRED` branch... has NO dedicated test~~ - **CLOSED Checkpoint 49 Part 23**: `test_reconciliation_required_when_square_off_succeeds_but_reconciliation_still_diverges` (monkeypatches the trigger module's own `reconcile_paper_state` name to a fixed non-zero divergence, isolating the branch's own routing logic rather than attempting to force a real inconsistency through `PaperBroker`'s API) - proves the event correctly lands in `RECONCILIATION_REQUIRED` (not `COMPLETED`, not `FAILED_RETRYABLE`), records the divergence count, and remains reclaimable by a later retry. See Decision 203. |
| ~~POS-003: `run_position_monitor_tick()` is not wired into any scheduled task~~ | `IMPLEMENTED_AND_TESTED` (closed Checkpoint 44) | `run_market_data_ingestion_tick()` now calls `run_position_monitor_tick(current_prices=...)` on every tick, using the SAME freshly-fetched quote prices the bar-aggregation path uses - independent of whether that tick also produced a new signal, so an open position is watched on every price update. Reconciliation now also triggers when an automatic exit fires, not only on a new entry. 2 new tests: a contract test proving zero positions means zero evaluation (no fabricated activity), and a full stop-loss-triggering scenario proving a real open position (with a real `ExitPlan` attached via `attach_exit_plan()`) is automatically exited within one scheduled tick, producing a second real paper order. Wrapped so a position-monitor failure can never mask ingestion success, mirroring the existing reconciliation-wrapping precedent. |
| ~~POS-001: Position management domain layer has no stateful orchestration~~ | `IMPLEMENTED_AND_TESTED` (closed Checkpoint 43) | `infrastructure/api/position_monitor_runtime.py::run_position_monitor_tick()` is the stateful `PositionMonitorService`: loads open `ManagedPosition`s from the ledger, evaluates `evaluate_position_exit()` (Checkpoint 42, reused verbatim), submits a REAL exit paper order through `PaperTradingService.submit_order()` for a fired `ExitDecision`, and persists the updated lifecycle/remaining-quantity/highest-favorable-price. Proven by ONE complete end-to-end test through the real orchestration path (`test_full_entry_to_monitored_exit_lifecycle_through_real_orchestration`): signal → entry order → fill → position with a real `ExitPlan` → 3 price updates → TARGET_1 (partial) → TARGET_2 (partial) → TARGET_3 (full close) → 4 total orders, complete audit trail. **Two real bugs found and fixed by this test** (see Decisions 191-192): a partial-exit-fraction basis bug that left rounding-drift residuals instead of closing exactly, and a Python `Decimal('0') or x` falsy-zero bug that would have resurrected a fully-closed position as still-open on its next load. |
| ~~POS-002: No strategy produces an `ExitPlan`~~ | `IMPLEMENTED_AND_TESTED` (closed Checkpoint 43, OPT-IN) | `application/services/exit_plan_policy.py::derive_default_exit_plan()` - an explicitly labelled `PROJECT_POLICY` (fixed-percentage SL/T1/T2/T3/trailing, NOT a Dhan/NSE/SEBI requirement, NOT strategy-native research) - is now available and wired into `PaperSignalExecutionService` via `apply_default_exit_plan=True` + an injected `exit_plan_attacher`. Still OFF by default (mirrors Decision 176's own opt-in pattern for `enforce_per_trade_risk_limit`) so the many already-passing Checkpoint 36-42 tests that assume "no exit plan" remain valid and undisturbed. **Honest limitation, unchanged from the checkpoint's own naming of it**: this is a fixed-percentage POLICY DEFAULT, not a volatility/ATR-aware or strategy-native model - real production exit semantics almost certainly need more than this, and this module says so in its own docstring rather than implying research-backed correctness it doesn't have. |
| **NEW (Checkpoint 42, ID: CONC-001): Concurrent scheduler execution protection** | Priority P0 | `IMPLEMENTED_AND_TESTED` — `infrastructure/scheduling/distributed_lock.py` (Django-cache-backed atomic acquire, Redis in production) now guards `run_market_data_ingestion_tick()`; 7 lock tests + 1 ingestion-tick contention test, all passing. **External dependency**: none (reuses the already-configured Redis cache backend). **Acceptance criteria**: met — two overlapping tick invocations cannot both proceed; proven directly. |
| **NEW (Checkpoint 43, ID: POS-003): `run_position_monitor_tick()` is not wired into any scheduled task** | Priority P0 | `IMPLEMENTABLE_NOW` — `run_market_data_ingestion_tick()` (Checkpoint 41) does NOT call `run_position_monitor_tick()` yet, so even though the position-monitoring machinery is now real and tested (POS-001/POS-002), nothing schedules it against live prices. **Next action**: pass the freshly-fetched quote prices from `run_market_data_ingestion_tick()` into `run_position_monitor_tick(current_prices=...)` after bar aggregation, same pattern as the active-loop trigger. **Acceptance criteria**: an open position with an `ExitPlan` automatically exits within one Celery Beat cycle (60s) of its exit condition being met by a REAL scheduled tick, not only a direct test call. |
| **NEW (Checkpoint 43): kill switch still does not force-close open positions** | Priority P0 (unchanged status, re-confirmed) | Position-management machinery now exists (POS-001/POS-002 closed this checkpoint) but was deliberately NOT connected to the kill switch - `RISK_HALT` exists as an `ExitReason` enum value (Checkpoint 42) but nothing ever constructs an `ExitDecision` with that reason. Remains `BLOCKED` pending the same reviewed design Checkpoint 41's register entry already named - now with real position-exit machinery available to implement it against, which did not exist before this checkpoint. |
| **NEW (Checkpoint 42, ID: RECON-001): Reconciliation now runs automatically after a paper order is submitted via the scheduled loop** | Priority P1 | `IMPLEMENTED_AND_TESTED` for the wiring; `IMPLEMENTED_BUT_NOT_VALIDATED` for "periodically"/"at session close"/"after recovery from disconnect" triggers, none of which exist yet. **Evidence**: `run_market_data_ingestion_tick()` calls `reconcile_paper_state()` whenever `active_loop_invocations > 0`, wrapped so a reconciliation failure can never mask ingestion success (mirrors the existing `refresh()` view's own "aggregation must never break refresh" precedent). **Next action**: a periodic (not order-triggered-only) reconciliation schedule, and an EOD-triggered one. |
| SEBI algo-trading framework compliance (Algo-ID, broker strategy registration) | `MISSING` — `EXTERNAL_DEPENDENCY` | `VERIFIED_SECONDARY / PRIMARY_CONFIRMATION_PENDING` (upgraded from Checkpoint 37's plain `VERIFIED_SECONDARY` — Checkpoint 38 confirmed the exact primary circular identity directly on `sebi.gov.in`: Circular SEBI/HO/MIRSD/MIRSD-PoD/P/CIR/2025/0000013, "Safer participation of retail investors in Algorithmic trading," dated 2025-02-04, with confirmed timeline-extension circulars — but the full text still did not render via fetch, so the specific technical provisions remain secondary-sourced. See `SEBI_ALGO_TRADING_PRIMARY_VERIFICATION.md`.) Still no Algo-ID/registration concept anywhere in this project — does not block PAPER trading. |
| ~~`RiskLimits.max_per_trade_risk` is configured but NEVER enforced~~ | `IMPLEMENTED_AND_TESTED` (closed Checkpoint 39, opt-in) | `evaluate_order_risk()` now has a real check: `PER_TRADE_RISK_UNKNOWN` when the caller cannot determine the risk (no stop loss - `ema_crossover`'s case), `MAX_PER_TRADE_RISK_EXCEEDED` when it's known and over the limit. Gated behind `enforce_per_trade_risk_limit` (default `False`) - see Decision 176 for why unconditional enforcement was deliberately not made the default this checkpoint. 4 dedicated tests. |
| ~~No daily trade-count limit and no instrument allow/deny list exist AT ALL~~ | `IMPLEMENTED_AND_TESTED` (closed Checkpoint 39, unconditionally active) | `max_daily_trades`/`current_daily_trade_count` and `allowed_instruments`/`denied_instruments` are real `RiskEvaluationContext` fields with real checks, both default to unrestricted (matching this checkpoint's own "backward compatible unless a caller opts in" pattern) but genuinely block when configured. 9 dedicated tests (`test_risk_engine_gap_closure.py`). No call site currently configures either - the check exists and is proven, but nothing yet sets `max_daily_trades`/`allowed_instruments` in production composition (`paper_trading_runtime.py`) - a follow-up wiring task, not a design gap. |
| **NEW (Checkpoint 39): market-session engine now holiday/weekend-aware for 2026, but still 2026-only** | `PARTIALLY_IMPLEMENTED` | `NSE_HOLIDAYS_2026` closes Checkpoint 23's "no holiday calendar" limitation for THIS year only - a date in 2027+ falls back to weekday-only classification (still correct for weekends, silently wrong for a real holiday). No generic multi-year calendar service exists. `SessionStatus` gained `CLOSING`/`HOLIDAY` but nothing outside `domain.session` yet reads the new states specially (e.g. the risk engine's `market_session_is_open` boolean collapses `CLOSING`/`HOLIDAY`/`CLOSED` into the same "not open" outcome - correct for blocking new orders, but does not yet distinguish "closed for the day" from "in the square-off window" anywhere consuming code cares about). |
| **NEW (Checkpoint 39): restart-safe signal dedup exists but is not wired into any composition root** | `IMPLEMENTED_BUT_NOT_VALIDATED` (in the sense of "not yet used outside a test") | `DjangoPaperLedgerRepository.load_processed_signal_ids()` is real, tested, and proven to prevent a duplicate order across a simulated restart - but nothing in `infrastructure/api/paper_trading_runtime.py` (or anywhere else) calls it yet, because no automatic trigger exists to call it FROM (see the still-open "no automated trigger" P0 row above). |

## P1

| Gap | Status | Evidence |
|---|---|---|
| Automatic end-of-session expiry scheduling | `PARTIALLY_IMPLEMENTED` | `PaperBroker.force_expire_end_of_session()` (Checkpoint 34) and its manual API trigger (Checkpoint 35) both work and are tested. Nothing calls it automatically at session close — an operator must remember to call it. Named as a gap since Checkpoint 32's runtime-architecture decision; still true. |
| Market-data feed classification is manual, not enforced | `PARTIALLY_IMPLEMENTED` | `AggregatedBarObservation` (Checkpoint 24A) carries quality metadata, but nothing in `paper_signal_execution.py` or `PaperTradingService` currently *reads* that classification and refuses `TRADING_GRADE`-only paths from being fed `SAMPLE_BAR` data, because no automatic feed exists yet (see P0 row above) to even attempt the mistake. The moment an automated trigger is built, this becomes a hard requirement, not a future one. |
| Instrument master coverage | `UNKNOWN` (re-confirmed, not re-audited this checkpoint) | Not re-investigated this checkpoint under time constraints; carried forward from Checkpoint 34's own finding rather than re-verified — flagged here so it is not silently dropped from tracking. |
| Session/holiday calendar correctness | `UNKNOWN` (re-confirmed, not re-audited this checkpoint) | Same as above — explicitly not re-verified this checkpoint; do not treat its absence from this register as "resolved." |
| Clock/time integrity monitoring (drift detection) | `DESIGNED_ONLY` | No implementation found this checkpoint via targeted `grep` for clock-drift/NTP-style checks; not deepened this checkpoint. |
| Observability / control plane | `PARTIALLY_IMPLEMENTED` | Existing capability-status UI components and read APIs (Checkpoint 33-35) provide *some* visibility (order/trade/position/funds state), but there is no dedicated operational dashboard (queue depth, last successful sync timestamp, error rates) as Part 15 envisions. Not built this checkpoint. |
| Frontend operator dashboard depth | `PARTIALLY_IMPLEMENTED` | `PaperTradingPage.tsx` (Checkpoint 35) is functional but oriented around order entry and raw table monitors, not an at-a-glance operational summary. Not rebuilt this checkpoint — correctly scoped as lower priority than the Part 4-6 core bridge under this checkpoint's own stated priorities. |
| **NEW (Checkpoint 37): most report catalogue types remain NOT_YET_IMPLEMENTED/PARTIAL** | `PARTIALLY_IMPLEMENTED` | 3 of 11 catalogued report types are `AVAILABLE` with real data (`BACKTEST_REPORT`, `MARKET_DATA_QUALITY_REPORT`, and the new `COMMUNICATION_DELIVERY_REPORT`); the rest — Signal Report, Portfolio Report, Risk Report, Production Report, Audit Report, System Health Report, Strategy Research Report — remain `PARTIAL`/`NOT_YET_IMPLEMENTED`/`PLANNED`. Checkpoint 37 Part 8 named 16 report types; this project's actual catalogue (`ReportType`, Checkpoint 32) only tracks 11, and only one of the ~14 report types Part 8 named that didn't already exist was implemented this checkpoint (Communication Delivery). Live Signal Report, Execution Report, Order Report, Trade Report, Daily P&L Report, Strategy-wise Win/Loss, Risk Rejection Report, Missed/Blocked Signal Report, and Broker Rejection Report were NOT built this checkpoint — real underlying data exists for several of them (paper orders/trades/positions since Checkpoint 35, risk decisions since Checkpoint 34) but no report assembler reads it yet. |
| **NEW (Checkpoint 37): frontend/CSS — no code changed this checkpoint** | `PARTIALLY_IMPLEMENTED` (unchanged from Checkpoint 35/36) | Part 9 asked for a full frontend audit (accessibility, responsive tables, status indicators beyond color). Not attempted this checkpoint — time was prioritized on the Signal Communication Engine (this checkpoint's core objective) and its tests. No frontend file was modified; Checkpoint 35's design-token/responsive work is the last frontend change on record. |

## P2

| Gap | Status | Evidence |
|---|---|---|
| Frontend/backend dependency vulnerabilities | `IMPLEMENTED_AND_TESTED` (as in: fully investigated and classified, not "fixed") | See `ACTIVE_PRODUCT_OPERATIONAL_RESEARCH.md` Part 3 — 5 npm advisories (all dev-only, unreachable given actual config) and 8 pip advisories (both packages dev-only). No forced upgrade applied; documented as a dedicated future tooling-upgrade checkpoint's job. |
| Accessibility contrast/keyboard walkthrough | `UNKNOWN` | Checkpoint 35 added `:focus-visible` and design tokens but no contrast-ratio measurement or full keyboard walkthrough was performed then or now. Explicitly not validated this checkpoint — reported as unknown rather than assumed fine. |
| Failure/recovery matrix (Part 16) | `DESIGNED_ONLY` | Not authored this checkpoint under time constraints. |
| Reporting depth (Part 20) | `PARTIALLY_IMPLEMENTED` | `REPORTING_ARCHITECTURE.md` (Checkpoint 32) exists; no new report types added this checkpoint — none were justified by new data this checkpoint produced beyond the signal-lineage fields already covered by `PaperOrderRecord.signal_id`. |

## P3

| Gap | Status | Evidence |
|---|---|---|
| Second strategy wired to paper trading | `BLOCKED` (deliberately) | See `STRATEGY_TO_PAPER_SELECTION.md` — withheld pending the same evidence bar `ema_crossover` cleared (independent reference validation), not a technical blocker. |
| Dhan Sandbox adapter-conformance harness | `EXTERNAL_DEPENDENCY` | Classified `USE_LATER`; correct trigger is the start of real `BrokerGateway` adapter work, not this checkpoint. |
| ~~Communication-ledger automatic retry~~ | `IMPLEMENTED_AND_TESTED` (closed Checkpoint 38) | `NotificationRouter` now retries a TRANSIENT failure up to `max_attempts` (default 3) with exponential backoff, classified per-provider (`is_retryable`); a PERMANENT failure (bad token/webhook) is never retried. Proven: `test_transient_failure_is_retried_and_eventually_succeeds`, `test_retry_is_bounded_and_gives_up_after_max_attempts`, `test_permanent_failure_is_never_retried`. |
| Communication engine — no automatic trigger for live signals | `DESIGNED_ONLY` (mirrors the paper-signal bridge's own P0 row) | Still not wired to any scheduler/live feed this checkpoint either — see the P0 "no automated trigger" row above; the same market-data-quality-enforcement prerequisite blocks both. |
| ~~Provider message-ID capture (Telegram/Discord)~~ | `IMPLEMENTED_AND_TESTED` (closed Checkpoint 38) | `send_telegram_message_with_id()` parses Telegram's real JSON response (`result.message_id`); `send_discord_message_with_id()` uses Discord's documented `?wait=true` query parameter to get the real message object back (Discord's default webhook POST returns 204 with no body). Both are `VERIFIED_PRIMARY` against each provider's own public API docs (unchanged from Checkpoint 22's original sourcing). Not yet exercised against REAL Telegram/Discord credentials in this session — proven only against the in-memory fake providers and JSON-shape assertions; see Part 22 honesty note in the final report. |
| **NEW (Checkpoint 38): paper-mode reconciliation now runs against REAL `PaperBroker` + ledger** | `IMPLEMENTED_AND_TESTED` | `infrastructure/api/paper_reconciliation_runtime.py::reconcile_paper_state()` composes the EXISTING broker-neutral `control_plane.reconciliation` engine (Checkpoint 34, previously only proven against synthetic fixtures) against a real `PaperBroker` + `DjangoPaperLedgerRepository`. 4 new tests, including one that directly corrupts a ledger row and proves the mismatch is detected (`test_ledger_drift_from_broker_is_detected`). Detection/classification only — no automatic correction, per Decision 152's unchanged principle. |
| **NEW (Checkpoint 38): signal identity model now includes timeframe + full strategy version** | `IMPLEMENTED_AND_TESTED` | `derive_signal_id()` extended from (strategy_id, configuration_version, instrument_id, timestamp) to also include timeframe, specification_version, code_version — closing the theoretical multi-timeframe collision gap Part 6 asked to justify. `direction` deliberately excluded (an output, not an input — see the function's own docstring for the full justification). |

## What changed since Checkpoint 34/35's registers

- **Closed this checkpoint**: nothing was fully closed to `IMPLEMENTED_AND_TESTED`
  at the "operates automatically in production" bar — the new bridge is
  real and tested but explicitly not wired to a live trigger (see P0).
  Overstating this as "done" would be exactly the dishonesty this
  checkpoint's principles forbid.
- **Newly identified this checkpoint**: the market-data-classification
  enforcement gap (P1) — did not exist as a distinct line item before,
  because no automated signal-to-order pathway existed for it to matter
  yet.
- **Explicitly not re-audited this checkpoint** (carried forward
  unchanged, flagged so silence isn't mistaken for resolution): instrument
  master coverage, session/holiday calendar correctness, clock-drift
  monitoring, accessibility validation, failure/recovery matrix.
