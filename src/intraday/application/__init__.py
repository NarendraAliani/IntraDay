# src/intraday/application/__init__.py
#
# Package boundary for the Application layer (Checkpoint 4). Orchestrates
# use-cases across bounded contexts and exposes contracts to the frontend
# (Checkpoint 1 Rule 13). Contains the health/version infrastructure
# endpoints (application/gateways/health.py) — the only real code in this
# checkpoint, deliberately infrastructure-only, no business gateways yet.
