# domain/feature

> Architecture placeholder — no business logic. Created during the foundational architecture checkpoint.

## Responsibility

Canonical feature/indicator contracts (feature_id, inputs, lookback, versioning) consumed identically by research and signal intelligence.

## Depends On

domain/shared_kernel, domain/market_data

## Must Not Depend On

Feature computation implementation

