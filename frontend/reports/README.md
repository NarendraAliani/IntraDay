# frontend/reports

> Architecture placeholder — no business logic. Created during the foundational architecture checkpoint.

## Responsibility

Reporting and analytics views.

## Depends On

application/contracts

## Must Not Depend On

Backend internals

## Checkpoint 32 update

The reporting/analytics UI itself lives at
`frontend/src/features/reports/` (`ReportsOverviewPage.tsx` and its
data files) - a `src/` feature directory, matching every other screen
in this project. This top-level `frontend/reports/` directory remains
the Checkpoint-1/2 output-artifact placeholder it always was, unused
by business logic.

