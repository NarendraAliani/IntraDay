# config/broker

> Architecture placeholder — no business logic. Created during the foundational architecture checkpoint.

## Responsibility

Broker connection configuration boundary (credentials referenced, never committed).

## Depends On

domain/broker

## Must Not Depend On

Hard-coded secrets in source

